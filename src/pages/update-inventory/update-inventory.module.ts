import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { UpdateInventoryPage } from './update-inventory';

@NgModule({
  declarations: [
    UpdateInventoryPage,
  ],
  imports: [
    IonicPageModule.forChild(UpdateInventoryPage),
  ],
})
export class UpdateInventoryPageModule {}
